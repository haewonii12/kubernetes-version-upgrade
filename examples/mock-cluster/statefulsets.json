{
  "items": [
    {
      "metadata": { "name": "prometheus", "namespace": "monitoring" },
      "spec": { "template": { "spec": { "containers": [{ "name": "prometheus", "image": "quay.io/prometheus/prometheus:v2.55.1" }] } } }
    }
  ]
}
